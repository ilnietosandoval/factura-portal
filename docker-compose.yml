version: "3.8"
services:
  factura-portal:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./invoices:/app/invoices
      - ./data:/app/data
    restart: unless-stopped
